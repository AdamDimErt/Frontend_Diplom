/** @format */

import { Instrument_Sans } from "next/font/google";

export const instrumentSans = Instrument_Sans({
    subsets: ["latin"],
    weight: ["400", "700"],
    variable: "--font-instrument-sans", // CSS-переменная
    display: "swap",
  });